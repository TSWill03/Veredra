{
  "name": "veredra-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test --grep-invert @private",
    "test:pwa": "playwright test tests/pwa.spec.mjs",
    "test:private": "playwright test --grep @private"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "fflate": "0.8.2"
  }
}
