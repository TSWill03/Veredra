# Signature: dev.tswicolly03
name: txt_webnovel_reader
description: MVP de leitor continuo para livros em .txt, com foco inicial em desktop.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  file_selector: ^1.1.0
  path: ^1.9.1
  path_provider: ^2.1.5
  shared_preferences: ^2.5.4
  archive: ^3.6.1
  epubx: ^4.0.0
  pdfrx: ^1.3.5
  image: ^3.3.0
  html: ^0.15.6
  markdown: ^7.3.0
  # 7.1+ requires Xcode 26.1.1; keep CI and current stable Xcode compatible.
  connectivity_plus: 7.0.0
  flutter_secure_storage: ^10.3.1
  idb_shim: 2.9.2
  supabase_flutter: ^2.16.0
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
